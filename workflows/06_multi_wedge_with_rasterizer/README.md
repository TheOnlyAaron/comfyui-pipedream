# 06 — Multi-Wedge with Rasterizer

Two wedges (prompt + seed) connected to a PD Rasterizer for dry-run preview. Shows the cartesian product iteration table and filename preview.

**Nodes used:** PD Wedge (×2), PD Rasterizer, PD Output Namer, KSampler, Save Image
