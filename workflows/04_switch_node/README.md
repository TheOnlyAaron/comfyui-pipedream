# 04 — Switch Node

Demonstrates single-channel and multi-channel switching. Shows manual mode (widget-driven index) and value mode (upstream INT drives selection).

**Nodes used:** PD Switch, CLIP Text Encode, KSampler, CheckpointLoaderSimple
